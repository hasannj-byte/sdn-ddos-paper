# Core ML / DL
tensorflow==2.13.0
scikit-learn>=1.3
imbalanced-learn>=0.11
xgboost>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Config / IO / plotting
pyyaml>=6.0
matplotlib>=3.7
joblib>=1.3
tqdm>=4.66
psutil>=5.9          # controller CPU/memory monitoring in run_testbed.py

# Note: the SDN testbed (Mininet, POX, Open vSwitch, sFlow) is installed at the
# system level inside the Ubuntu guest, NOT via pip. See README.md, "Testbed setup".
